import PublicLayout from './(root)/layout';
import LandingPage from './(root)/page';

const RootPage = () => {
    return (
        <PublicLayout>
            <LandingPage />
        </PublicLayout>
    );
};

export default RootPage;
