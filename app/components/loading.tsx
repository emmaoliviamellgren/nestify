const Loading = () => {
    return (
        <div className='h-screen w-screen flex items-center justify-center'>
            <p className='title text-[--secondary] animate-pulse'>Loading...</p>
        </div>
    );
};

export default Loading;
